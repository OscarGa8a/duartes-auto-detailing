---
import PageHero from "../../ui/PageHero.astro";
import ContactSection from "./ContactSection.astro";
import ServiceMap from "./ServiceMap.astro";
import type { ContactContent, ContactContentWithServiceMap } from "../../../i18n/content-types";

interface Props {
  content: ContactContent;
}

const hasServiceMap = (content: ContactContent): content is ContactContentWithServiceMap => content.policy.serviceMap === "include";
const { content } = Astro.props;
---

<PageHero
  title={content.hero.title}
  backgroundImage={{ mobile: "banner-contact-mobile_oh3t4c", desktop: "banner-contact-web_ngbhya" }}
  eyebrow={content.hero.eyebrow}
  description={content.hero.description}
/>
<ContactSection content={content} />
{hasServiceMap(content) && <ServiceMap content={content.map} />}
